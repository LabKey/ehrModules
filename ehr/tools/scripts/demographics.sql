SELECT id, CurDate() AS Date, (sex) AS sex, (dam) AS dam, (sire) AS sire, (birth) AS birth, (acquire) AS acquire, 
(labanimals.exit) AS "exit", (acqcode) AS acqcode, (exitcode) AS exitcode, ('') AS name, ('') AS info1, ('') AS info2 FROM labanimals
